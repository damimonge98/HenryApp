import React from 'react';
import { LoadingWrapper, LoadingCircle } from './styles';

const Loading = () => {
  return (
    <LoadingWrapper>
      <LoadingCircle />
    </LoadingWrapper>
  );
};

export default Loading;
